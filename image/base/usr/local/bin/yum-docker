#!/bin/bash

install() {
    microdnf \
        --nodocs \
        --setopt=install_weak_deps=0 \
        -y \
        install ${@}
}

remove() {
    microdnf -y remove ${@}
}

clean() {
    microdnf -y clean all
}

quick-install() {
    install ${@}
    clean
}

${@}
