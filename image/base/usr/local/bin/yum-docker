#!/bin/bash

install() {
    microdnf -y install ${@}
}

remove() {
    microdnf -y remove ${@}
}

clean() {
    microdnf -y clean all
}

update() {
    microdnf -y update
    clean
}

quick-install() {
    install ${@}
    clean
}

${@}
