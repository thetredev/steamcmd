ARG REGISTRY_BASE_IMAGE
FROM ${REGISTRY_BASE_IMAGE}

# Define a default SRCDS environment for Counter-Strike
ENV STEAMCMD_SERVER_GAME=cstrike \
    STEAMCMD_SERVER_MAP=de_dust2 \
    STEAMCMD_SERVER_SESSION_NAME=cs
