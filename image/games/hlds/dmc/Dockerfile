ARG REGISTRY_BASE_IMAGE
FROM ${REGISTRY_BASE_IMAGE}

# Define a default HLDS environment for Deatchmatch Classic
ENV STEAMCMD_SERVER_APP_CONFIG="+app_set_config 90 mod dmc" \
    STEAMCMD_SERVER_GAME=dmc \
    STEAMCMD_SERVER_MAP=dmc_dm2

ENV STEAMCMD_SERVER_SESSION_NAME=${STEAMCMD_SERVER_GAME}
