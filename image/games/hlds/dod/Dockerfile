ARG REGISTRY_BASE_IMAGE
FROM ${REGISTRY_BASE_IMAGE}

# Define a default HLDS environment for Day of Defeat
ENV STEAMCMD_SERVER_APP_CONFIG="+app_set_config 90 mod dod" \
    STEAMCMD_SERVER_GAME=dod \
    STEAMCMD_SERVER_MAP=dod_donner

ENV STEAMCMD_SERVER_SESSION_NAME=${STEAMCMD_SERVER_GAME}
