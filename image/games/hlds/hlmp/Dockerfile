ARG REGISTRY_BASE_IMAGE
FROM ${REGISTRY_BASE_IMAGE}

# Define a default HLDS environment for Half Life
ENV STEAMCMD_SERVER_GAME=valve \
    STEAMCMD_SERVER_MAP=c1a1 \
    STEAMCMD_SERVER_SESSION_NAME=hlmp
