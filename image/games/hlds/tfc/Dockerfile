ARG REGISTRY_BASE_IMAGE
FROM ${REGISTRY_BASE_IMAGE}

# Define a default HLDS environment for Team Fortress Classic
ENV STEAMCMD_SERVER_APP_CONFIG="+app_set_config 90 mod tfc" \
    STEAMCMD_SERVER_GAME=tfc \
    STEAMCMD_SERVER_MAP=2fort

ENV STEAMCMD_SERVER_SESSION_NAME=${STEAMCMD_SERVER_GAME}
