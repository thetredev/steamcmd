ARG REGISTRY_BASE_IMAGE
FROM ${REGISTRY_BASE_IMAGE}

# Define a default SRCDS environment for Black Mesa: Deathmatch
ENV STEAMCMD_SERVER_APPID=346680
ENV STEAMCMD_SERVER_GAME=bms
ENV STEAMCMD_SERVER_MAP=dm_bounce
ENV STEAMCMD_SERVER_SESSION_NAME=${STEAMCMD_SERVER_GAME}
