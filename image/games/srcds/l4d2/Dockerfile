ARG REGISTRY_BASE_IMAGE
FROM ${REGISTRY_BASE_IMAGE}

# Define a default SRCDS environment for Left 4 Dead 2
ENV STEAMCMD_SERVER_APPID=222860 \
    STEAMCMD_SERVER_GAME=left4dead2 \
    STEAMCMD_SERVER_MAP=c2m1_highway

ENV STEAMCMD_SERVER_SESSION_NAME=${STEAMCMD_SERVER_GAME}
